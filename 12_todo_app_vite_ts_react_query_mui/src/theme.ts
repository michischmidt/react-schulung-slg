import { createTheme } from "@mui/material";

export const SLG_PRIMARY = "#00AEEF";

const theme = createTheme({
  palette: {
    primary: {
      main: SLG_PRIMARY,
    },
  },
});

export default theme;
